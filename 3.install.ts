// console.log("just")   -> tsc 3.install.ts -> gives a 3.install.js file -> then we execute this js file by node 3.install.js 
 /* 
const user = {
    name:"vinit",
    age:21
}

console.log(user.firstName) -> gives red swiggly still can produce the js file
*/

//we write a lot of code in typescript but it is safer code